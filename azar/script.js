const updateScrollGlow=()=>{const height=document.documentElement.scrollHeight-window.innerHeight;document.body.style.setProperty("--scroll-y",`${height>0?window.scrollY/height*100:0}%`)};window.addEventListener("scroll",updateScrollGlow,{passive:true});window.addEventListener("resize",updateScrollGlow);updateScrollGlow();const canvas=document.querySelector("#background-nodes");if(canvas){const ctx=canvas.getContext("2d"),nodes=[],reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;const make=()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.38,vy:(Math.random()-.5)*.38,r:Math.random()*1.5+.8});const resize=()=>{const scale=Math.min(devicePixelRatio||1,2);canvas.width=innerWidth*scale;canvas.height=innerHeight*scale;ctx.setTransform(scale,0,0,scale,0,0);nodes.length=0;Array.from({length:Math.min(70,Math.max(28,Math.floor(innerWidth/24)))},make).forEach(n=>nodes.push(n))};const draw=()=>{ctx.clearRect(0,0,innerWidth,innerHeight);nodes.forEach((n,i)=>{if(!reduced){n.x+=n.vx;n.y+=n.vy;if(n.x<0||n.x>innerWidth)n.vx*=-1;if(n.y<0||n.y>innerHeight)n.vy*=-1}ctx.beginPath();ctx.arc(n.x,n.y,n.r,0,Math.PI*2);ctx.fillStyle="rgba(255,141,240,.48)";ctx.fill();nodes.slice(i+1).forEach(o=>{const d=Math.hypot(n.x-o.x,n.y-o.y);if(d<145){ctx.beginPath();ctx.moveTo(n.x,n.y);ctx.lineTo(o.x,o.y);ctx.strokeStyle=`rgba(141,183,255,${.16*(1-d/145)})`;ctx.stroke()}})});if(!reduced)requestAnimationFrame(draw)};addEventListener("resize",resize);resize();draw()}
